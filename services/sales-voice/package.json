{
  "name": "@yad/sales-voice",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Production Outbound Sales voice service. Separate process from the inbound receptionist and from the demo line; shares only the approved Twilio number.",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^6.0.3",
    "@types/ws": "^8.5.13"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
