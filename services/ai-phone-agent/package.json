{
  "name": "@yad/ai-phone-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Live AI phone demo line — one number that becomes the right industry intake agent.",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "node --experimental-strip-types --watch src/server.ts",
    "start": "node --experimental-strip-types src/server.ts",
    "test": "node --experimental-strip-types --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/ws": "^8.5.13",
    "typescript": "^6.0.3"
  }
}
